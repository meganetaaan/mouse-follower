{
	"name": "mouse-follower-workspace",
	"private": true,
	"version": "1.0.0",
	"description": "Workspace for mouse-follower library and demo",
	"author": "meganetaaan",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/meganetaaan/mouse-follower.git"
	},
	"homepage": "https://meganetaaan.github.io/mouse-follower/",
	"engines": {
		"node": ">=20.19.0"
	},
	"type": "module",
	"scripts": {
		"dev": "pnpm --filter demo dev",
		"build": "pnpm --filter mouse-follower build && pnpm --filter demo build",
		"build:lib": "pnpm --filter mouse-follower build",
		"build:demo": "NODE_ENV=production GITHUB_PAGES=true pnpm --filter demo build",
		"test": "pnpm --filter mouse-follower test",
		"check:fix": "biome check --fix",
		"publish:lib": "pnpm --filter mouse-follower publish"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.1.3",
		"@semantic-release/changelog": "^6.0.3",
		"@semantic-release/git": "^10.0.1",
		"semantic-release": "^23.0.0"
	}
}
